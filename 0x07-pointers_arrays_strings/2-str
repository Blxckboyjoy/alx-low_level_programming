#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * 
 */
char *_strchr(char *s, char c)
 char *s = "hello";
    char *f;

    f = _strchr(s, 'l');

    if (f != NULL)
    {
        printf("%s\n", f);
    }
    return (0);