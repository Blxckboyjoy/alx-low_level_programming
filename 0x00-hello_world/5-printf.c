#include <stdio.h>

/**
 * main - entry point
 * description ;uses printf function
 * return - always zero
 */

int main(void)
{
printf("%s", "with proper grammar, but the outcome is a piece of art,\n");

return (0);
}
