#include "main.h"

/**
 * main - checks code
 * return -0
 */
int main(void)
{
    char*str;

    str = "0123456789";
    puts2(str);
    }
            if char*str = odd;
    {
    printf(lenght_of_the_string - 1)\2;
    }

    return(0);
}
